services:
  - type: web
    name: jarvis-voice-assistant
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: WEATHER_API_KEY
        sync: false
